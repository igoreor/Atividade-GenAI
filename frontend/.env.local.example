# Não é necessário alterar — o proxy do Next.js já roteia /api/* para localhost:8000.
# Se o backend rodar em outra porta, ajuste next.config.mjs > rewrites > destination.
NEXT_PUBLIC_API_URL=http://localhost:8000
